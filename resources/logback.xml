<!-- Logback configuration. See http://logback.qos.ch/manual/index.html -->
<configuration scan="true" scanPeriod="10 seconds">
  <!-- Console output -->
  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <!-- encoder defaults to ch.qos.logback.classic.encoder.PatternLayoutEncoder -->
    <encoder>
      <charset>UTF-8</charset>
      <pattern>%boldWhite(%date{yyyy-MM-dd'T'HH:mm:ss.SSSXXX, GMT}) %white([PITFALL]) %highlight(%-5level) %cyan(%logger{36}) - %msg%n</pattern>
    </encoder>
    <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
      <level>INFO</level>
    </filter>
  </appender>

  <appender name="FILE" class="ch.qos.logback.core.FileAppender">
    <file>/var/log/pitfall/application.log</file>
    <append>true</append>
    <immediateFlush>true</immediateFlush>
    <encoder>
      <charset>UTF-8</charset>
      <!-- no color output for file -->
      <pattern>%date{yyyy-MM-dd'T'HH:mm:ss.SSSXXX, GMT} [PITFALL:%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>

    <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
      <level>INFO</level>
    </filter>
  </appender>

  <!-- Enable FILE and STDOUT appenders for all log messages.
       By default, only log at level INFO and above. -->
  <root level="INFO">
    <appender-ref ref="STDOUT" />
    <appender-ref ref="FILE" />
  </root>

  <!-- For loggers in the these namespaces, log at all levels. -->
  <logger name="user" level="ALL" />
  <!-- To log pedestal internals, enable this and change ThresholdFilter to DEBUG -->
  <logger name="io.pedestal" level="WARN" />
</configuration>
